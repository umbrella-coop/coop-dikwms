/brainstorm
how instruct XLM (X is any acronmy prefix, Large Model) agents to prefer deterministic aproaches without lossing the advanteges of stochastic aproach? E.g. scrapy selector and fallback to a LLM to handle the parse and output the structured data 