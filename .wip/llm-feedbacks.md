Recommendation (small, actionable):
- Make docs/specs/backlog.md the single intake queue: every brainstorm outcome, user request, dependency, and relationship-reference gets appended there (SPEC-004 must be added!)
- At the start of each implementation cycle, check the backlog first (a "what's next" gate) — the SDD DISCUSS phase should reference the backlog item being promoted
- Optionally add a roadmap doc (docs/roadmap.md) with phases mapped to specs, so "on demand" becomes "from a curated queue"

Also should I ACT: add SPEC-004 to the backlog now (it's the missing one)? Yes — that's a concrete fix aligned with my recommendation. Let me add SPEC-004 to backlog.md quickly, then answer.
