{
  "name": "dikwms-frontend",
  "type": "module",
  "scripts": {
    "dev": "bit run app -p 3100",
    "watch": "bit watch",
    "test": "bit test",
    "build": "bit build app",
    "check": "node scripts/check-layout.mjs",
    "e2e": "node e2e/run-e2e.mjs",
    "e2e:gate": "bash scripts/e2e-gate.sh",
    "fix-links": "bit install --add-missing-deps"
  }
}
