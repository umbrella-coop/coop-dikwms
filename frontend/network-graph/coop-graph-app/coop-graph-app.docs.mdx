---
description: 'A coop-graph-app app.'
labels: ['app', 'react']
---

A React application for coop-graph-app. 

## Run the application

Import the app and add the following config to your workspace.jsonc:

```
bit use coop-graph-app 
```

To run the app locally, run the following:

```
bit run coop-graph-app 
```
