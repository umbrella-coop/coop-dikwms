---
description: A Announcements UI component.
---

A UI component for displaying announcements for the Acme corporation.

## Get started

Use the component by importing and rendering the Announcements component:

```js
<Announcements />
```

## Testing

During testing the compoennt will display mock data. To get mock data to be rendered make sure to wrap the component with the apollo `MockedProvider`.
