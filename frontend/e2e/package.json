{
  "name": "dikwms-e2e",
  "private": true,
  "version": "0.1.0",
  "description": "Real-browser E2E for the dikwms git explorer (SPEC-027) — Playwright + chrome-headless-shell against the live stack",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.0"
  }
}
